{
  "name": "cf-yashu-agent",
  "version": "1.0.0",
  "description": "A minimal AI chat agent built with Cloudflare Agents SDK",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

